# 2026-07-10

- 开发 `Hd_CameraModule_3DKeyenceLJS3`：基于 `Hd_CameraModule_3DKeyence3` 接口新增 LJS8 3D 基恩士模块，添加头文件、实现文件和模块 CMake；移植 LJS8 SDK 的以太网连接、高速通信、触发、回调取图、队列取图、导出函数和参数 UI；调整顶层 CMake 使 `keyence3DLJS8` 链接 `LJS8_IF`。
