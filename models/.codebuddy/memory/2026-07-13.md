# 2026-07-13

- 修改 `Hd_CameraModule_HIK3` 硬触发回调：SDK 图像回调中按采集时的 `Currentindex` 克隆图像并入异步队列，由独立 worker 调用外部回调，避免外部回调阻塞相机 SDK 回调线程；同时为 `CallbackFuncMap` 增加互斥保护并用 `QPointer` 降低对象销毁风险。尝试构建 `Hd_CameraModule_HIK3` 时当前环境找不到 `cmake`。
