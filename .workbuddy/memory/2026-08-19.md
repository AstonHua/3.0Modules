# 2026-08-19 工作日志

## 任务：HIK 模块封装范式研究 + sdk-module-codegen 技能生成

**研究对象**：`models/Hd_CameraModule_HIK3`（海康 MV SDK），对照 `DaHua3`/`Dalsa3` 确认同族范式。

**范式要点（后续生成模块必须遵守）**：
- 5 层结构：extern "C" 导出层（create/destroy/getCameraWidgetPtr/getCameraPtr/getCameraSnList + TotalMap 注册表）→ 门面层（`Hd_CameraModule_XXX3 : PbGlobalObject`，实现 7 个虚接口）→ SDK 层（`CameraFunSDKfactoryCls`：枚举/连接/重连/异步回调线程 generation 防悬垂）→ UI 层（`mPrivateWidget`）→ GlobalInclude 底座。
- 配置：每设备 `<SN>.json`（GetOnceImageTimes/OnceImageCounts/OnceSignalsGetImageCounts/ExposureTime/Gain/Gamma 序列 "index,value"）+ ParasValueMap + setParameter/upDateParam 生效。
- 状态出口统一 `trigged(int)`：0 重连成功/1 掉线/2 软触发/3 硬触发/501 预处理结束/1000 重置计数；init 内 10/11 控触发模式。
- 图像流：SDK 回调线程深拷贝 QByteArray 入异步队列 → worker 解码 cv::Mat → 按 cameraIndex 分发 PBGLOBAL_CALLBACK_FUN；软触发 = setData(空) → TriggerSoftware → data() 阻塞取。
- 构建：模块 CMake DLL_NAMES 声明依赖，顶层 CMake env/ 自动发现；调用方 QLibrary::resolve 零耦合。

**交付**：安装用户级技能 `~/.workbuddy/skills/sdk-module-codegen/`（SKILL.md + references 2 份 + templates 5 份 + examples/hik-minimal-demo 演示）。安全自查 P2。与已有 `hd-qt-module-template`（算法模块 Base 基类）互补不冲突。

**备注**：本环境 ToolSearch 无 SkillManage，技能通过直接写目录文件安装，frontmatter 含 `agent_created: true`。

## 任务：相机模块稳定性与资源安全审查（编程专家@code-review）

**审查范围**：Hd_CameraModule_HIK3（1545 行）/ DaHua3（1661 行）/ Dalsa3（1303 行）+ GlobalInclude 11 个基础组件。全部只读审查，未改码。

**高危结论（Top）**：
1. HIK L287 & Dalsa L399：`Currentindex >= getImageMaxCoiunts / OnceGetImageNum` 除零（OnceImageCounts 可配 0）→ SIGFPE。
2. HIK L91-97：MV_CC_CreateHandle 未检查返回值；connctDevice 失败分支 handle 悬垂/泄漏（CurrentCamera->handle 仍指已释放句柄，重试 use-after-free）。
3. HIK L157-192：ReconnectDevice 30s 阻塞重连与模块析构无同步 → 析构中重连线程访问已释放对象。
4. HIK 异步队列（AsyncCallbackQueue）与 MatQueue 均无容量上限 + 每帧深拷贝 → 高帧率 OOM 风险。
5. DaHua L303/295：deviceOnLine 重连用 `IMV_AttachGrabbing(..., NULL)` + pUser 类型混淆（this↔handle）→ 重连后回调空指针/UB 崩溃；L804 create 失败未 delete（内存泄漏）；L181 triggerOffBack 空函数指针。
6. Dalsa 质量最好（特征层判空/范围校验齐全），仅除零与回调 QPointer 缺失。
7. GlobalInclude：AlgParm modelTreeView 的 QJsonModel* 泄漏（new 无 parent 析构不删）；qjsonmodel flags()/setData 无效 index 空指针（疑似）；ThreadSafeQueue DisableWakeup 无锁数据竞争。

**已沉淀到技能**：sdk-module-codegen 的 SKILL.md「自检清单」应补充除零防护（OnceGetImageNum 下限 1）、handle 判空、回调 QPointer 校验——下次生成模板时已在 cpp.tpl 中体现 qMax(…,1) 与判空。（模板已含这些防护）

**遗留**：未对 3DKeyence3/3DLMI3/RealSense3 等其它模块审查（不在本轮范围）。修复建议清单见对话交付。

## 任务：修复 Top1 除零 + Top2 DaHua 重连崩溃链（18:18-18:40）

**前置发现**：实际源文件比审查时副本更新——HIK upDateParam 已有 qMax、DaHua create() 已有 delete temp.base（用户已自行修复部分项）。修复前先核实当前状态，避免重复改。

**Top1 除零（完成）**：
- HIK `ImageCallBackEx` 轮转：`const int divisor = qMax(OnceGetImageNum,1)` + `getImageMaxCoiunts > 0` 条件；upDateParam timeOut 加 `qMax(…,10)`。
- Dalsa `XferCallback` 轮转同 HIK 方案；setParameter 三参数全部 qMax 下限。
- DaHua upDateParam getImageMaxCoiunts/timeOut 补 qMax（OnceGetImageNum 已有）。

**Top2 DaHua 重连崩溃链（完成）**：
- `onDeviceLinkNotify`：pUser 统一为 CameraFunSDKfactoryCls*（原首次传 this、重连传 handle 的类型混淆已修），handle 从 `currentUser->handle` 取并判空。
- `deviceOnLine`：do-while(true) 改 30 次限次重试，失败 qCritical 返回；重连后 AttachGrabbing/SubscribeConnectArg 的 pUser 均传 currentUser（原传 NULL → onGetFrame 空指针必崩）。
- `onGetFrame`：pFrame/pUser/currentUser/handle 四重判空；triggerOffBack 判空。
- `initSdk`：失败分支统一 cleanupOnFail（Stop→Close→Destroy+置空），消除句柄泄漏。
- create() delete 原本已有，未动。
- **刻意未动**：DaHua initSdk 成功 emit trigged(1) 的信号语义（M11，与其 init() lambda 内部约定自洽），避免本次引入行为变化；已在代码注释标注遗留。

**验证**：备份于 models/.review_backup/*.cpp.bak；diff 全量核对三文件（HIK 8 行/DaHua 146 行/Dalsa 12 行变更），无无关改动。编译验证需用户实际环境（缺 Qt/opencv/SDK 头文件）。
